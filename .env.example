# ===================================================================
# == Archivo de ejemplo para variables de entorno                  ==
# == Copia este archivo a .env y rellena los valores necesarios.   ==
# ===================================================================

# ---------------------------------
# -- Configuración Obligatoria
# ---------------------------------

# URL del webhook de n8n al que se reenviarán los mensajes entrantes.
# Ejemplo: https://orionglobal.app.n8n.cloud/webhook/288c...
N8N_WEBHOOK_URL=

# URL pública de la Cloud Function que n8n usará para el callback.
# Ejemplo: https://us-central1-mi-proyecto.cloudfunctions.net/chatHandler
CALLBACK_URL=

# ---------------------------------
# -- Configuración Opcional
# ---------------------------------

# Texto que se envía al usuario mientras el flujo de n8n está procesando el mensaje.
PLACEHOLDER_TEXT="*Pensando... 🤖*"

# Mensaje de error genérico mostrado al usuario si el flujo falla de forma definitiva.
ERROR_FALLBACK_TEXT="⚠️ Lo siento, hubo un problema al procesar tu mensaje. Por favor, inténtalo de nuevo."

# Nivel de log de la función. Puede ser `error`, `warn`, `info`, `debug`.
LOG_LEVEL=info

# Tiempo máximo de espera (en milisegundos) para la respuesta de n8n.
N8N_TIMEOUT_MS=60000

# Número máximo de reintentos cuando la llamada a n8n falla.
N8N_MAX_RETRIES=3

# Tiempo de espera (en milisegundos) entre cada reintento hacia n8n.
N8N_RETRY_DELAY=2000